const chai = require('chai');
const sinon = require('sinon');
const expect = chai.expect();

describe('DB functions', () => {
	describe('db.connect', () => {
		it('should create a pool without error and call callback');
	});
	describe('db.get', () => {
		it('should return a null pool');
	});
});
