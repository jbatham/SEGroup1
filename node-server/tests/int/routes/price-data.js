const chai = require('chai');
const chaiHttp = require('chai-http');
const get_location_radius = require('../../../controllers/get-prices');
const expect = chai.expect();

describe('Price route integrations', () => {
	it('should respond to hitting /price-data/get');
});
